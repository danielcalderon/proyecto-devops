apiVersion: v1
kind: Service

metadata:
  name: proyecto-devops-app
  namespace: proyecto-devops

spec:
  selector:
    app: proyecto-devops-app

  ports:
    - port: 80
      targetPort: 8080

  type: NodePort
